# Core FastAPI dependencies
fastapi==0.95.2
uvicorn[standard]==0.22.0
python-multipart==0.0.6
pydantic==1.10.9

# Scientific computing - minimal versions for Heroku
numpy==1.21.6
opencv-python-headless==4.5.5.64  # Use headless version (no GUI dependencies)
Pillow==9.5.0

# TensorFlow - using 2.8.0 which is smaller than 2.11.0 but still compatible
# This version is more compact and should help reduce slug size
tensorflow==2.8.0
protobuf==3.19.6
