# Core FastAPI dependencies
fastapi==0.95.2
uvicorn[standard]==0.22.0
python-multipart==0.0.6
pydantic==1.10.9

# Scientific computing - minimal versions for Heroku
numpy==1.21.6
opencv-python-headless==4.5.5.64  # Use headless version (no GUI dependencies)
Pillow==9.5.0

# TensorFlow - using 2.6.0 CPU-only for smaller size (compatible with code)
# CPU-only version is significantly smaller than full TensorFlow
tensorflow-cpu==2.6.0
protobuf==3.19.6
